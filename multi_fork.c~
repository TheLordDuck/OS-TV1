#include <stdio.h>
#include <unistd.h>
#include <sys/wait.h>
#include <stdlib.h>

int main(void)
{
  pid_t son_pid1 = -1;
  pid_t son_pid2 = -1;
  pid_t son_pid3 = -1;
  son_pid1 = fork();
  son_pid2 = frok();
  son_pid3 = fork();
  
  printf("(%d) Allors ", getpid());
  sleep(1);
  printf("(%d) I miss my daddy! my father is (%d).\n", getpid(), getppid());
  sleep(10);
  exit(EXIT_SUCCESS);
}
